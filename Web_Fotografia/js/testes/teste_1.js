let limite = 100;
let saldo_atual = 10000;

let resultado = (saldo_atual < limite) ? "Necessito depositar $" : "Não necessita Depósito"

alert(resultado)

/* Sintaxe:
let variavel = (condição) ? //instrução se true ://instrução se false 

*/